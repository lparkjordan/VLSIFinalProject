`switch default
pmos #(0.1)   (* const real width = 3.000, length = 0.600; *) M1( y,
     cds_globals.vdd_, a);
`switch default
nmos #(0.1)   (* const real width = 2.100, length = 0.600; *) M0( y,
     cds_globals.gnd_, a);

endmodule
